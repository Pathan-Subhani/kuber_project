---
 apiVersion: v1
 kind: Pod
 metadata:
  name: postgre-pod
  labels:
   name: postgre-pod
   app: demo-voting-app
 spec:
  containers:
   - name: postgre
     image: postgres:9.4
     ports:
     - containerPort: 5432
...

